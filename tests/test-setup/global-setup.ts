/**
 * global-setup.ts: Sets up global state before all tests start.
 * Add any initialization setup code within this function.
 */

export default (): void => {
  // Add any initialization setup here
};
